# Database
DATABASE_URL="postgresql://kullanici:sifre@localhost:5432/qr_menu_db?schema=public"
# IMPORTANT: Direct (non-pooled) connection for migrations
DATABASE_URL_UNPOOLED="postgresql://kullanici:sifre@localhost:5432/qr_menu_db?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
# IMPORTANT: Must be at least 32 random characters. Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="CHANGE_ME_generate_with_openssl_rand_base64_32"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Customer Device Block HMAC Secret
# IMPORTANT: Must be at least 32 random characters. Generate with: openssl rand -base64 32
CUSTOMER_DEVICE_HMAC_SECRET="CHANGE_ME_generate_with_openssl_rand_base64_32"
